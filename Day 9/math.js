/**
 * Named Exports: Export multiple items from a module by their names.
   Default Exports: Export one item as the default for a module.
 */

const add = (a, b) => a + b;
const sub = (a, b) => a - b;

module.exports = {add,sub};
